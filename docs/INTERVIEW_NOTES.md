# Interview Notes

- The solution preserves the existing folder structure and upgrades it progressively.
- LangGraph is used as a multi-step workflow rather than a single prompt.
- AI outputs are editable and never auto-approved.
- The backend degrades safely when Groq is unavailable so the build remains demo-ready.
